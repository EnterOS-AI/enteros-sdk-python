# Code generated by tools/gen-python — DO NOT EDIT.
#
# Source of truth: mcp/mcp-plugin-delivery.contract.json (validated against
# mcp/mcp-plugin-delivery.schema.json). Every value below is DERIVED from that
# contract instance, never hand-written. Regenerate with:
#
#     node tools/gen-python.mjs
#
# A CI drift gate (.gitea/workflows/codegen-drift.yml) re-runs the generator and
# fails if this file differs from a fresh regeneration, per RFC molecule-core#3285 §14.

# Derived scalar string constants from the MCP plugin-delivery contract.

# REQUIRED_TOOL is derived from contract field "required_tool": the singular tool verb the concierge management MCP must expose; the degrade gate fails closed if it is absent on the live surface (the #3082 phantom-verb invariant).
REQUIRED_TOOL = "provision_workspace"

# MCP_SERVER_NAME is derived from contract field "mcp_server_name": logical name of the management-mode MCP server descriptor.
MCP_SERVER_NAME = "molecule-platform"

# SETTINGS_KEY is derived from contract field "key": top-level key inside the settings file under which MCP servers are declared.
SETTINGS_KEY = "mcpServers"

# DEFAULT_SETTINGS_PATH is derived from contract field "settings_path": default (claude_code) settings file path the rendered MCP descriptor is written to.
DEFAULT_SETTINGS_PATH = "/configs/.claude/settings.json"

# LOADED_MCP_TOOLS_FIELD is derived from contract field "loaded_mcp_tools_field": name of the field reporting the set of tools actually loaded from the MCP server.
LOADED_MCP_TOOLS_FIELD = "loaded_mcp_tools"

# RUNTIME_PRESENT_FIELD is derived from contract field "runtime_present_field": name of the runtime-reported boolean field indicating the MCP server is present.
RUNTIME_PRESENT_FIELD = "mcp_server_present"

# LEGACY_BINARY_PATH is derived from contract field "legacy_binary_path": filesystem path of the legacy MCP server binary older presence probes checked.
LEGACY_BINARY_PATH = "/opt/molecule-mcp-server"

__all__ = [
    "REQUIRED_TOOL",
    "MCP_SERVER_NAME",
    "SETTINGS_KEY",
    "DEFAULT_SETTINGS_PATH",
    "LOADED_MCP_TOOLS_FIELD",
    "RUNTIME_PRESENT_FIELD",
    "LEGACY_BINARY_PATH",
]
